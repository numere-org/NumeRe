Fixed	Die neuen "string()" Dimensionen sollten nun in Schleifen und Verzweigungen funktionieren.
Fixed	"to_uppercase()" interferiert nicht mehr mit Kontrollzeichen: "\n" und "\t"
Fixed	"clear string(DIM)" kann nun auch fuer eine beliebige DIM des "string()"-objekts ausgefuehrt werden. 
Added	"getindices()" kann nun auch mit den neuen "string()" Dimensionen umgehen.
New	"string()" hat eine neue Dimension: es koennen nun beliebig viele "string()"-Objekte in Form von Spalten angelegt werden.
New	NumeRe kann *.xls-Dateien lesen.
New	NumeRe kann in *.xls-Dateien exportieren.
Fixed	"showf" zaehlt nun nicht mehr die Zeilen der Spaltenueberschriften mit.
Cleaned	"getfilelist()" und "getfolderlist()" loesen in Verbindung mit Platzhaltern inmitten des Pfades die speziellen Ordner "/.." und "/." nicht mehr auf.
Fixed	globale Zeichenkettenvariablen werden in Prozeduren von "str" wieder erkannt => zusaetzlich die Erkennung von Zeichenkettenvariablen verbessert.
Fixed	Strings koennen nun wieder bei "progress" verwendet werden.
Fixed	Zeilenumbrueche koennen nun auch in "throw" verwendet werden.
Added	Fortschrittsanzeigen koennen nun mit "type=cancel" bzw. "type=bcancel" auch abgebrochen werden.
New	NumeRe kann nun *.xlsx-Dateien lesen.
Cleaned	"clear string()" funktioniert nun wie "clear cache()". Fuer die einzelnen Dimensionen wurde "delete string(DIM)" eingefuehrt.
Fixed	Stack overflow beim Lesen groesserer Dateien mit "read" wurde behoben.
Fixed	"*ticklabels" kann nun mit Umlauten umgehen.
Added	NumeRe kann nun auch Dateien mit mehrzeiligen Spaltentiteln (weitestgehend) korrekt einlesen.
Fixed	Kleineres Problem beim Plotten von Caches mit "*cache()" als Namen wurde behoben.
Fixed	NumeRe konnte bestimmte Varianten des ODS-Dateiformates nicht korrekt lesen. Wurde behoben.
Cleaned	Die Position der Darstellung des Filters bei "list -files" richtet sich nun automatisch nach der Dateitypspalte direkt darunter.
Fixed	In seltenen Faellen erkannte "data -paste" nicht Zahlen und Ueberschriften, wenn diese haendisch eingegeben wurden. Wurde behoben.
Cleaned	NumeRe beachtet nun beim Plotten die Reihenfolge der Funktionen und Datensaetze.
Cleaned	"plot3d" kann nun auch Datensaetze mit nur zwei Spalten darstellen.
Added	Die Option "axisbind" kann verwendet werden, um Funktionen und Datensaetze an verschiedene Achsen zu binden.
Added	"define" kann nun rekursive Definitionen durch die Option "recursive" ausfuehren. Dabei werden alle bestehenden Funktionsaufrufe in der neuen Definition durch die vorherige Definition ersetzt.
Added	Mit der Option "asval={VARS}" koennen variablen vorgegeben werden, die waehrend einer Funktionsdefinition durch ihre Werte ersetzt werden sollen.
Cleaned	Es koennen nun auch Funktionendefinitionen in Schleifen und Verzweigungen durchgefuehrt werden.
Added	NumeRe besitzt nun ein "Arbeitsverzeichnis", das durch "<wp>" indiziert wird und mittels "workpath PATH" fuer die aktuelle Sitzung geaendert werden kann.
Fixed	Ein kleiner Bug beim automatischen Laden des Caches wurde behoben.
